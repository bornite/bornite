packages:
  - "apps/*"
  - "packages/*"

# Native/telemetry post-install build scripts are declined; not needed here.
allowBuilds:
  "@scarf/scarf": false
  esbuild: false
  sharp: false
  unrs-resolver: false
