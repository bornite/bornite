# Copy to apps/api/.env to run the API against Postgres.
# Without DATABASE_URL the API falls back to an in-memory store (zero setup).
DATABASE_URL=postgres://bornite:bornite@localhost:5432/bornite

# DB_SYNCHRONIZE=true   # auto-create the schema from the entities (dev default)
# DB_LOGGING=false
# PORT=3001
